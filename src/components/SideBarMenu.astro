---
const { sideBarActiveItemID } = Astro.props
const selectedPageClass = "bg-base-content/10"
---

<ul class="menu grow shrink menu-md overflow-y-auto">
  <li>
    <a
      class={`py-3 my-0.5 text-base hover:bg-base-content/10 active:bg-base-content/10 ${sideBarActiveItemID === "home" ? selectedPageClass : ""}`}
      id="home"
      href="/"
      >Home</a
    >
  </li>
  <li>
    <a
      class={`py-3 my-0.5 text-base hover:bg-base-content/10 active:bg-base-content/10 ${sideBarActiveItemID === "bio" ? selectedPageClass : ""}`}
      id="bio"
      href="/bio"
      >Bio</a
    >
  </li>
  <li>
    <a
      class={`py-3 my-0.5 text-base hover:bg-base-content/10 active:bg-base-content/10 ${sideBarActiveItemID === "lessons" ? selectedPageClass : ""}`}
      id="lessons"
      href="/lessons"
      >Lessons</a
    >
  </li>
  <li>
    <a
      class={`py-3 my-0.5 text-base hover:bg-base-content/10 active:bg-base-content/10 ${sideBarActiveItemID === "music" ? selectedPageClass : ""}`}
      id="music"
      href="/music"
      >Music</a
    >
  </li>
  <li>
    <a
      class={`py-3 my-0.5 text-base hover:bg-base-content/10 active:bg-base-content/10 ${sideBarActiveItemID === "contact" ? selectedPageClass : ""}`}
      id="contact"
      href="/contact"
      >Contact</a
    >
  </li>
</ul>
